# Changelog

## Unreleased

- Docs site in English and Spanish, playground palette swatches show the real color, fixed dark mode samples and playground layout.

## 0.1.0

- Core: `toRuns`, `bitmapSize`, `cssUnit`, `toSvg` and the `Bitmap`, `Palette`, `Run` types.
- React: `<PixelGlyph>` from `@kreeptales/pixel-glyph/react`.
